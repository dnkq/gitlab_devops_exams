apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders
  template:
    metadata:
      labels:
        app: orders
    spec:
      containers:
        - name: orders
          image: dnkq/orders:latest
          ports:
            - containerPort: 8000
